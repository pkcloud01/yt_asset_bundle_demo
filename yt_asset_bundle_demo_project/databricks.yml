# This is a Databricks asset bundle definition for yt_asset_bundle_demo_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: yt_asset_bundle_demo_project
  uuid: 2d964182-85a9-4925-aaca-eb854b7c6383

artifacts:
  python_artifact:
    type: whl
    build: python setup.py bdist_wheel


include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-06b7825e-48b1.cloud.databricks.com
      profile: DEV_PROFILE
      root_path: /Workspace/Users/pkcloudacademy@gmail.com/.bundle/${bundle.name}/${bundle.target}

  prod:
    mode: production
    workspace:
      profile: PROD_PROFILE
      host: https://dbc-06b7825e-48b1.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/pkcloudacademy@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/pkcloudacademy@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: pkcloudacademy@gmail.com
        level: CAN_MANAGE
