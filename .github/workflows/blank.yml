# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "master" branch
  push:
    branches:
      - master

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: databricks/setup-cli@main
      - run: databricks bundle deploy -t dev --bundle-root yt_asset_bundle_demo_project/
        env:
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          DATABRICKS_BUILD_ENV: dev
        

      # # Runs a single command using the runners shell
      # - name: Run a one-line script
      #   run: echo Hello, world!
      # - name: Set up Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: "3.11"

      # - name: Install Databricks CLI (latest binary)
      #   run: |
      #     curl -fsSL https://raw.githubusercontent.com/databricks/setup-cli/main/install.sh | bash
      #     databricks version

      # - name: Authenticate Databricks
      #   env:
      #     DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
      #     DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
      #   run: |
      #     databricks auth profiles set --profile ci --host "$DATABRICKS_HOST" --token "$DATABRICKS_TOKEN"
      #     databricks auth switch --profile ci
      #     databricks auth whoami

      # - name: Validate and Deploy Bundle
      #   run: |
      #     databricks bundle validate -t dev --profile ci
      #     databricks bundle deploy -t dev --profile ci
          
      # - run: pip install flake8
      # - run: flake8 --max-line-length=100 yt_asset_bundle_demo_project/src
      # - name: Authenticate Databricks (v2)
      #   env:
      #     DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
      #     DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
      #   run: |
      #     databricks auth profiles set --profile ci --host "$DATABRICKS_HOST" --token "$DATABRICKS_TOKEN"
      #     databricks auth switch --profile ci
      #     databricks auth whoami

      # - name: Validate and Deploy Bundle
      #   run: |
      #     databricks bundle validate -t dev --profile ci
      #     databricks bundle deploy -t dev --profile ci

      # # Runs a set of commands using the runners shell
      # - name: Run a multi-line script
      #   run: |
      #     echo Add other actions to build,
      #     echo test, and deploy your project.
